<h1>Listing households</h1>
Search by member names, then make an investment or purchase.
<%= form_tag households_path, :method => 'get' do %>
  <fieldset>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
    <p class="help">
      match by first, last, or multiple names, e.g. "Juan", "Juan Verde", "Juan Juanita":
    </p>
  </fieldset>
<% end %>
<% if @households.empty? %>
 <p>There are no households matching your criteria.</p>
<% else %>
<table id="households">
  <tr>
    <th>Members</th>
    <th>Balance</th>
    <th></th>
  </tr>

<% @households.each do |household| %>
  <tr class="<%= cycle("even", "odd") %>">
    <td>
      <ol style="padding-top: 0px; padding-bottom: 0px; margin: 0px;">
      <% household.members.each do |member| %>
      	<li><%= member %></li>
      <% end %>
      </ol>
    </td>
    <td><%= sprintf("$%.2f", household.balance) %></td>
    <td><%= link_to 'Investment/Purchase', household %></td>
  </tr>
<% end %>
</table>
<% end %>
<br />

